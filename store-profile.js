const form=document.querySelector('#profile'),status=document.querySelector('#status'),save=document.querySelector('#save');
async function call(action,payload={}){const token=localStorage.getItem('loyalty_merchant_access_token');if(!token)throw Error('Sign in to your business owner account first.');const r=await fetch('/api/neon',{method:'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+token},body:JSON.stringify({action,...payload})});const d=await r.json();if(!r.ok)throw Error(d.error||'Unable to save listing');return d;}
call('directory_profile_get').then(d=>{for(const key of ['city','state','address'])form.elements[key].value=d.profile[key]||'';form.elements.listed.checked=d.profile.listed===true;save.disabled=false;status.textContent='Country: United States';}).catch(e=>status.textContent=e.message);
form.addEventListener('submit',async e=>{e.preventDefault();save.disabled=true;try{await call('directory_profile_save',{city:form.elements.city.value,state:form.elements.state.value,address:form.elements.address.value,listed:form.elements.listed.checked});status.textContent='Listing saved.';}catch(e){status.textContent=e.message;}finally{save.disabled=false;}});
